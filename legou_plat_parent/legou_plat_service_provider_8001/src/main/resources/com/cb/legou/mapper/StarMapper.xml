<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cb.legou.mapper.StarMapper">

    <select id="findByName" parameterType="com.cb.legou.domain.Star" resultType="com.cb.legou.domain.Star">
        SELECT * from t_star WHERE name=#{name} AND password=#{password}
    </select>


</mapper>